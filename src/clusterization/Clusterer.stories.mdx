import { Meta, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Clusterer as ClustererCmp } from './Clusterer';
import { YMaps, Map, Clusterer, Placemark } from '../';
import points from './__fixtures__/points';

<Meta title="api/Clusterization/Clusterer" />

# Clusterer

Renders Yandex.Maps API [Clusterer][docs]

## Basic Usage

<Canvas>
  <YMaps>
    <Map
      defaultState={{
        center: [55.751574, 37.573856],
        zoom: 5,
      }}
    >
      <Clusterer
        options={{
          preset: 'islands#invertedVioletClusterIcons',
          groupByCoordinates: false,
        }}
      >
        {points.map((coordinates, index) => (
          <Placemark key={index} geometry={coordinates} />
        ))}
      </Clusterer>
    </Map>
  </YMaps>
</Canvas>

## Props

<ArgsTable of={ClustererCmp} />

[docs]:
  https://tech.yandex.com/maps/doc/jsapi/2.1/ref/reference/Clusterer-docpage/
